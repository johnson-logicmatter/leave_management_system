Hi <%= @applier.name %>,
   <%= @approver.name %> has approved your <%= truncate_trailing_zero @leave.no_of_days %> day(s) <%= @leave_type.downcase %> applied on following dates. And it'll be processed shortly.

Dates are:
<% @leave.leave_dates_object.each do |ldate| %>
   <%= ldate.to_date.strftime "%b %d, %Y" %> - <%= ldate.match(/:F$/ ) ? "Full day" : "Half day" %>
<% end %>